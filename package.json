{
  "name": "ai-gftd-news",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@gftd/magatama-host-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  },
  "scripts": {
    "cljs:release": "cd clj && amu compile --target wasm32-browser worker",
    "cljs:watch": "cd clj && amu compile --target wasm32-browser worker",
    "typecheck": "tsc --noEmit"
  }
}
